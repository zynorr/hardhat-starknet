"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.NETWORK_ALIASES = exports.DEFAULT_CONFIG = void 0;
exports.resolveConfig = resolveConfig;
exports.resolveNetworkUrl = resolveNetworkUrl;
/**
 * Runtime defaults for the Starknet plugin configuration.
 */
exports.DEFAULT_CONFIG = {
    network: "devnet",
    scarbPath: "scarb",
    profile: "release",
    artifactDir: "target/dev",
};
/**
 * Known network aliases that resolve to RPC URLs.
 */
exports.NETWORK_ALIASES = {
    devnet: "http://127.0.0.1:5050",
    sepolia: "https://sepolia.starknet.io",
    mainnet: "https://alpha-mainnet.starknet.io",
};
/**
 * Resolve a user config (with defaults) into a complete runtime config.
 * Merges user-provided values with sane defaults.
 *
 * @param userConfig - Partial user config from hardhat.config.ts
 * @returns Fully resolved config with all fields set
 */
function resolveConfig(userConfig) {
    return {
        network: userConfig?.network ?? exports.DEFAULT_CONFIG.network,
        scarbPath: userConfig?.scarbPath ?? exports.DEFAULT_CONFIG.scarbPath,
        profile: userConfig?.profile ?? exports.DEFAULT_CONFIG.profile,
        packageName: userConfig?.packageName,
        artifactDir: userConfig?.artifactDir ?? exports.DEFAULT_CONFIG.artifactDir,
        wallet: userConfig?.wallet &&
            (userConfig.wallet.accountAddress !== undefined ||
                userConfig.wallet.privateKey !== undefined)
            ? userConfig.wallet
            : {
                accountAddress: process.env.STARKNET_ACCOUNT,
                privateKey: process.env.STARKNET_PRIVATE_KEY,
            },
    };
}
/**
 * Resolve a network alias to its full RPC URL.
 * Returns the input unchanged if it's already a URL.
 *
 * @param network - Network alias or URL
 * @returns Full RPC URL
 * @throws If the network alias is unknown and it's not a URL
 */
function resolveNetworkUrl(network) {
    if (network.startsWith("http://") || network.startsWith("https://")) {
        return network;
    }
    const url = exports.NETWORK_ALIASES[network];
    if (!url) {
        throw new Error(`Unknown Starknet network: "${network}". ` +
            `Use one of: ${Object.keys(exports.NETWORK_ALIASES).join(", ")}, or a full RPC URL.`);
    }
    return url;
}
//# sourceMappingURL=resolve.js.map