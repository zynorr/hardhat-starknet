"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("hardhat/config");
const plugins_1 = require("hardhat/plugins");
const PLUGIN_NAME = "hardhat-starknet";
/**
 * `compile:starknet` — Compile Cairo contracts using Scarb.
 */
(0, config_1.task)("compile:starknet", "Compile Cairo contracts with Scarb").setAction(async (_, hre) => {
    console.log("🔨 Compiling Cairo contracts...");
    const result = await hre.starknet.compile();
    if (result.success) {
        console.log("✅ Cairo contracts compiled successfully");
        if (result.output) {
            console.log(result.output);
        }
    }
    else {
        throw new plugins_1.HardhatPluginError(PLUGIN_NAME, `Compilation failed:\n${result.output}`);
    }
});
/**
 * `starknet:build` — Alias for compile:starknet.
 */
(0, config_1.task)("starknet:build", "Build Cairo contracts with Scarb (alias for compile:starknet)").setAction(async (_, hre) => {
    await hre.run("compile:starknet");
});
//# sourceMappingURL=build.js.map