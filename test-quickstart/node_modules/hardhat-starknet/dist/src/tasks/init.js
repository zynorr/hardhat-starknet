"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const config_1 = require("hardhat/config");
/**
 * `starknet:account` — Show current account info.
 */
(0, config_1.task)("starknet:account", "Show current Starknet account information").setAction(async (_, hre) => {
    const url = hre.starknet.getProviderUrl();
    console.log(`🌐 Starknet RPC URL: ${url}`);
    try {
        const addr = hre.starknet.getAccountAddress();
        console.log(`👤 Account address: ${addr}`);
    }
    catch (e) {
        console.log(`⚠️  No account configured. ${e.message}`);
    }
});
/**
 * `starknet:create-account` — Create and deploy a new Starknet account.
 */
(0, config_1.task)("starknet:create-account", "Create and deploy a new Starknet account (devnet)")
    .addOptionalParam("fund", "Amount of wei to fund the new account with (default: 1 ETH = 10^18 wei)", undefined, config_1.types.string)
    .setAction(async ({ fund: fundString }, hre) => {
    console.log("=".repeat(50));
    console.log("  🆕 Starknet Account Creation");
    console.log("=".repeat(50));
    const result = await hre.starknet.createAccount(fundString);
    console.log("\n" + "=".repeat(50));
    console.log("  ✅ New Starknet Account Created");
    console.log("=".repeat(50));
    console.log(`  📬 Address:    ${result.address}`);
    console.log(`  🔐 Public key:  ${result.publicKey}`);
    console.log(`  🔑 Private key: ${result.privateKey}`);
    console.log("=".repeat(50));
    console.log("\nSet these environment variables to use this account:");
    console.log(`  export STARKNET_ACCOUNT=${result.address}`);
    console.log(`  export STARKNET_PRIVATE_KEY=${result.privateKey}`);
    return result;
});
/**
 * `starknet:info` — Show project info from Scarb.toml.
 */
(0, config_1.task)("starknet:info", "Show Cairo project info from Scarb.toml").setAction(async (_, hre) => {
    const manifest = await hre.starknet.parseManifest();
    if (!manifest) {
        console.log("⚠️  No Scarb.toml found in the current directory.");
        return;
    }
    console.log("📋 Starknet/Cairo Project Info");
    console.log("=".repeat(50));
    console.log(`  📦 Package:    ${manifest.name} v${manifest.version}`);
    console.log(`  📐 Edition:    ${manifest.edition}`);
    console.log(`  🔗 Dependencies:`);
    for (const [dep, ver] of Object.entries(manifest.dependencies)) {
        console.log(`     - ${dep}: ${ver}`);
    }
    console.log(`  🎯 Sierra:     ${manifest.sierra}`);
    console.log(`  🎯 CASM:       ${manifest.casm}`);
    if (manifest.starknetContracts.length > 0) {
        console.log(`  📝 Contracts:  ${manifest.starknetContracts.join(", ")}`);
    }
    console.log("=".repeat(50));
});
/**
 * `starknet:list-contracts` — List compiled contracts from artifacts.
 */
(0, config_1.task)("starknet:list-contracts", "List all compiled contract artifacts").setAction(async (_, hre) => {
    const contracts = await hre.starknet.listContracts();
    if (contracts.length === 0) {
        console.log("⚠️  No compiled contracts found. Run `compile:starknet` first.");
        return;
    }
    console.log("📁 Compiled Contracts:");
    for (const name of contracts) {
        const artifacts = await hre.starknet.findArtifacts(name);
        if (artifacts) {
            console.log(`  📄 ${name}`);
            console.log(`     Sierra: ${artifacts.sierra.split("/").pop()}`);
            console.log(`     CASM:   ${artifacts.casm.split("/").pop()}`);
        }
    }
});
//# sourceMappingURL=init.js.map