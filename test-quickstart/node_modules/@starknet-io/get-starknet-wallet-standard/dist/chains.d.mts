type ChainId = `0x${string}`;
declare const STARKNET_CHAIN_PREFIX = "starknet:";
type StarknetChain = `${typeof STARKNET_CHAIN_PREFIX}${ChainId}`;
declare class StarknetChainError extends Error {
    constructor(message: string);
}
declare const WELL_KNOWN_STARKNET_CHAINS: ["starknet:0x534e5f4d41494e", "starknet:0x534e5f5345504f4c4941"];
/** Check if a chain identifier is a Starknet chain. */
declare function isStarknetChain(chain: string): chain is StarknetChain;
/** Returns a `StarknetChain` from a `ChainId`. */
declare function formatStarknetChainId(chainId: string): StarknetChain;
/** Returns the 0x prefixed chain id. */
declare function getStarknetChainId(chain: string): ChainId;

export { type ChainId, STARKNET_CHAIN_PREFIX, type StarknetChain, StarknetChainError, WELL_KNOWN_STARKNET_CHAINS, formatStarknetChainId, getStarknetChainId, isStarknetChain };
