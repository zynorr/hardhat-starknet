import { RequestFn } from '@starknet-io/types-js';
import { WalletWithFeatures, Wallet } from '@wallet-standard/base';
import { StandardConnectFeature, StandardDisconnectFeature, StandardEventsFeature } from '@wallet-standard/features';
export { StandardConnect, StandardConnectFeature, StandardDisconnect, StandardDisconnectFeature, StandardEvents, StandardEventsChangeProperties, StandardEventsFeature, StandardEventsListeners, StandardEventsNames, StandardEventsOnMethod, StandardEventsVersion } from '@wallet-standard/features';

declare const StarknetWalletApi = "starknet:walletApi";
type StarknetWalletApiVersion = "1.0.0";
/** A Wallet Standard feature for wallets that implement Starknet's wallet API. */
type StarknetWalletRequestFeature = {
    readonly [StarknetWalletApi]: {
        readonly version: StarknetWalletApiVersion;
        readonly request: RequestFn;
        readonly walletVersion: string;
        readonly id: string;
    };
};

type StarknetFeatures = StarknetWalletRequestFeature & StandardConnectFeature & StandardDisconnectFeature & StandardEventsFeature;
type WalletWithStarknetFeatures = WalletWithFeatures<StarknetFeatures>;
declare const RequiredStarknetFeatures: ["starknet:walletApi", "standard:connect", "standard:disconnect", "standard:events"];
/** Check if a wallet is a Starknet wallet. */
declare function isStarknetWallet(wallet: Wallet): wallet is WalletWithStarknetFeatures;

export { RequiredStarknetFeatures, type StarknetFeatures, StarknetWalletApi, type StarknetWalletApiVersion, type StarknetWalletRequestFeature, type WalletWithStarknetFeatures, isStarknetWallet };
